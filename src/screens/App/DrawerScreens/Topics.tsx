import { View, Text } from "react-native";
import React from "react";
import Layout from "../../Layout";
import { Box } from "../../../components";

const Topics = () => {
  return (
    <Layout>
      <Box backgroundColor={"primary"} flex={1}></Box>
    </Layout>
  );
};

export default Topics;
